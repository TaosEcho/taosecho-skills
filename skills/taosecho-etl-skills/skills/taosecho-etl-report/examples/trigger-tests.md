# 触发测试

1. 明确指定：基于 state.md 生成标准报告。
2. 模式选择：我想要 brief 模式的简报。
3. 阶段判断完成后：跑完 dev-decision 后生成报告。
4. 缺字段：state.md 只有 2 项分析，能生成 brief 吗？
5. 多格式：生成 Markdown 和 PDF。
6. 无 host 工具：只生成 Markdown 即可。
7. 全字段：生成 full 模式包含 state.md 完整字段。
